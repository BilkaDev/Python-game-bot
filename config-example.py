# y,x,h,w
# localization_hp_bar = (y,x,h,w)
# localization_attack_target = (y,x,h,w)

# image_hp_target_wh = (w, h)

# item_bp_last_slot = (x, y)
# last_item_in_bp_location = [(985, 544), (986, 535), (988, 526), (997, 535)]